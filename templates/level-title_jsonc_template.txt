{
  "long_name": "${level_title}",
  "iso_name": "${LEVELTITLE}",
  "nickname": "${level_NICKNAME}", // 3 char name, all uppercase
  "gltf_file": "custom_levels/${level_title}/${level_title}.glb",
  "automatic_wall_detection": ${automatic_wall_detection},
  "automatic_wall_angle": ${automatic_wall_angle},

  "double_sided_collide": ${double_sided_collide},

  "base_id": 101,

  "ambients": [],

  "actors" : [
  ]
}